class Solution {
public:
    int poorPigs(int buckets, int minutesToDie, int minutesToTest) {
        int np = 0;
        while(pow(minutesToTest/minutesToDie+1,np)<buckets)
            np ++;
        return np;
    }
};
